# 题目属性快速参考表

## 🎯 核心属性（智能纠错必用）

| 属性名 | 类型 | 作用 | 关键程度 |
|--------|------|------|----------|
| `correct` | Boolean | 答案是否正确（`false` 需要纠错） | ⭐⭐⭐⭐⭐ |
| `questionType` | String | 题目类型：0=单选，1=多选，2=判断，3=填空，4=简答 | ⭐⭐⭐⭐⭐ |
| `stuAnswer` | String | 学生提交的答案 | ⭐⭐⭐⭐⭐ |
| `id` | String | 题目实例唯一ID（用于缓存key） | ⭐⭐⭐⭐⭐ |
| `questionOptionList` | Array | 选项列表（含 `isTrue` 判断正确答案） | ⭐⭐⭐⭐⭐ |
| `questionContent` | String | 题目内容（HTML格式） | ⭐⭐⭐⭐ |

---

## 📝 题目标识类

| 属性名 | 类型 | 作用 |
|--------|------|------|
| `id` | String | 题目记录唯一标识（实例ID） |
| `questionId` | String | 题目库唯一标识（模板ID） |
| `questionContent` | String | 题目内容（HTML） |
| `questionContentText` | String | 题目纯文本 |

---

## 🎯 答案相关类

| 属性名 | 类型 | 作用 |
|--------|------|------|
| `stuAnswer` | String | 学生答案（不同题型格式不同） |
| `answer` | String | 正确答案（通常为空，从选项提取） |
| `questionOptionList` | Array | 选项列表，通过 `isTrue: true` 找正确答案 |

---

## ✅ 批改结果类

| 属性名 | 类型 | 作用 |
|--------|------|------|
| `correct` | Boolean | **是否答对**（纠错判断核心） |
| `stuScore` | Number | 学生得分 |
| `score` | Number | 题目满分 |

---

## 🔢 题目类型

| questionType 值 | 题型 | 说明 |
|----------------|------|------|
| `"0"` | 单选题 | 只能选一个选项 |
| `"1"` | 多选题 | 可以选多个选项 |
| `"2"` | 判断题 | 对/错 |
| `"3"` | 填空题 | 填空形式 |
| `"4"` | 简答题 | 文本回答 |
| `"5"` | 编程题 | 代码题 |

---

## 💡 智能纠错流程中的属性使用

```
1️⃣ 筛选错题
   └─ 使用: correct === false

2️⃣ 确定题型
   └─ 使用: questionType (0/1/2/3/4)

3️⃣ 提取正确答案
   └─ 使用: questionOptionList 遍历查找 isTrue: true 的选项

4️⃣ 获取学生答案
   └─ 使用: stuAnswer

5️⃣ 缓存管理
   └─ 使用: id 作为缓存key
   └─ 使用: stuAnswer 作为已尝试值

6️⃣ 计算下一个答案
   └─ 使用: questionType + 已尝试答案列表
```

---

## 📊 不同题型的 stuAnswer 格式

| 题型 | stuAnswer 示例 | 说明 |
|------|----------------|------|
| 单选题 | `"A"` | 单个字母 |
| 多选题 | `"A,B"` 或 `"A"` | 逗号分隔或单个 |
| 判断题 | `"对"` 或 `"错"` | 中文判断词 |
| 填空题 | `"【csrf()】"` | 包含占位符 |
| 简答题 | 长文本 | 完整的文本内容 |

---

## 🔍 questionOptionList 结构

```json
[
  {
    "text": "A、选项内容",
    "isSelcted": true,    // 学生是否选中
    "isTrue": false       // 是否为正确答案 ⭐
  }
]
```

**关键**：通过 `isTrue: true` 找到正确答案！

---

## ⚠️ 注意事项

1. ✅ **纠错判断**：只看 `correct === false` 的题目
2. ✅ **正确答案提取**：优先从 `questionOptionList` 中 `isTrue: true` 的选项提取
3. ✅ **缓存key**：使用 `id` 字段，不是 `questionId`
4. ✅ **答案格式**：不同题型的 `stuAnswer` 格式不同
5. ✅ **空值处理**：很多字段可能为 `null`，需要判断

---

## 🚀 快速查找

### 如何判断是否需要纠错？
```javascript
if (item.correct === false) {
  // 需要纠错
}
```

### 如何提取正确答案？
```javascript
// 方法1: 从 questionOptionList 提取（推荐）
const correctOptions = item.questionOptionList
  .filter(opt => opt.isTrue === true)
  .map((opt, idx) => String.fromCharCode(65 + idx)); // A, B, C...

// 方法2: 直接从 answer 字段（备选）
const answer = item.answer;
```

### 如何确定题目类型？
```javascript
const typeMap = {
  "danxuan": "0",   // 单选
  "duoxuan": "1",   // 多选
  "panduan": "2",   // 判断
  "tiankong": "3",  // 填空
  "jianda": "4"     // 简答
};
```

