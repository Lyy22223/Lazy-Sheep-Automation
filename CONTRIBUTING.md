# 🤝 贡献指南

感谢你对懒羊羊自动化平台的关注！我们欢迎各种形式的贡献。

## 📋 贡献方式

### 🐛 报告Bug
1. 在 [Issues](https://github.com/Lyy22223/Lazy-Sheep-Automation/issues) 页面创建新issue
2. 使用清晰的标题描述问题
3. 提供详细的复现步骤
4. 附上截图或错误日志

### 💡 提出新功能
1. 先在 [Discussions](https://github.com/Lyy22223/Lazy-Sheep-Automation/discussions) 讨论
2. 说明功能的使用场景和价值
3. 等待维护者反馈后再开始开发

### 🔧 提交代码
1. Fork 本项目
2. 创建特性分支：`git checkout -b feature/amazing-feature`
3. 提交更改：`git commit -m '[feat] Add some amazing feature'`
4. 推送到分支：`git push origin feature/amazing-feature`
5. 创建 Pull Request

## 📝 提交规范

我们使用约定式提交（Conventional Commits）：

```
[类型] 简短描述

详细描述（可选）
```

### 类型标签
- `[feat]` - 新功能
- `[fix]` - Bug修复
- `[docs]` - 文档更新
- `[style]` - 代码格式（不影响功能）
- `[refactor]` - 重构代码
- `[perf]` - 性能优化
- `[test]` - 测试相关
- `[chore]` - 构建/工具链

### 示例
```bash
git commit -m "[feat] 添加超星学习通平台支持"
git commit -m "[fix] 修复多选题答案解析错误"
git commit -m "[docs] 更新API文档"
```

## 🏗️ 开发流程

### 1. 环境准备

**前端（油猴脚本）**
```bash
cd lazy-sheep-userscript
npm install
npm run dev  # 开发模式（自动重载）
```

**后端（API服务）**
```bash
cd lazy-sheep-backend
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install -r requirements.txt
uvicorn main:app --reload
```

### 2. 代码规范

**前端代码规范**
```bash
npm run lint    # 检查代码规范
npm run format  # 自动格式化
```

**后端代码规范**
```bash
black .         # 格式化代码
flake8 .        # 检查规范
```

### 3. 测试

**前端测试**
```bash
npm test        # 运行测试
npm run test:watch  # 监听模式
```

**后端测试**
```bash
pytest          # 运行测试
pytest --cov    # 测试覆盖率
```

## 🎯 开发指南

### 前端架构

```
lazy-sheep-userscript/
├── src/
│   ├── core/           # 核心功能（配置、状态管理）
│   ├── platforms/      # 平台适配器
│   │   ├── czbk/       # 传智播客平台
│   │   └── chaoxing/   # 超星学习通（待开发）
│   ├── modules/        # 功能模块（答题、刷课）
│   ├── network/        # 网络请求
│   └── ui/             # 用户界面
```

### 添加新平台支持

1. 在 `src/platforms/` 创建新平台目录
2. 实现平台适配器接口：
```javascript
export default class NewPlatformAdapter {
    detect() { /* 检测当前是否为该平台 */ }
    extractQuestions() { /* 提取题目 */ }
    fillAnswer(question, answer) { /* 填充答案 */ }
    submit() { /* 提交试卷 */ }
}
```
3. 在 `src/platforms/index.js` 注册平台
4. 添加测试用例
5. 更新文档

### 后端架构

```
lazy-sheep-backend/
├── api/
│   ├── routes/         # API路由
│   ├── services/       # 业务逻辑
│   └── models/         # 数据模型
├── core/               # 核心功能
└── tests/              # 测试用例
```

### 添加新API端点

1. 在 `api/routes/` 创建路由文件
2. 在 `api/services/` 实现业务逻辑
3. 添加API文档注释（自动生成OpenAPI）
4. 编写测试用例
5. 更新API文档

## 🔍 代码审查标准

### 必须通过
- ✅ 所有测试通过
- ✅ 代码规范检查通过
- ✅ 无明显的性能问题
- ✅ 有适当的注释和文档
- ✅ 提交信息符合规范

### 推荐
- 📝 添加或更新测试用例
- 📚 更新相关文档
- 🎨 遵循现有代码风格
- ♻️ 保持代码简洁可维护

## 💬 社区

- **GitHub Issues**: 报告Bug和功能请求
- **GitHub Discussions**: 讨论和问答
- **QQ群**: 待更新

## 📜 许可证

本项目采用 [MIT License](LICENSE) 开源协议。

贡献代码即表示你同意将你的代码以MIT协议开源。

---

再次感谢你的贡献！❤️
