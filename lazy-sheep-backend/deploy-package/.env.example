# ========================================
# 懒羊羊后端 - 环境配置模板
# ========================================

# 服务配置
APP_NAME="懒羊羊题库API"
APP_VERSION="1.0.0"
DEBUG=false
PORT=8000

# ========================================
# 数据库配置（二选一）
# ========================================

# 方式1: SQLite（简单部署，无需安装数据库）
DATABASE_URL=sqlite+aiosqlite:///./data/questions.db

# 方式2: PostgreSQL（生产环境推荐，性能更好）
# 需要先安装PostgreSQL并创建数据库
# DATABASE_URL=postgresql+asyncpg://lazy_user:your_password@localhost:5432/lazy_sheep

# Redis
REDIS_URL=redis://localhost:6379/0
REDIS_ENABLED=true

# DeepSeek AI
DEEPSEEK_API_KEY=sk-your-deepseek-api-key-here
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-chat

# API认证
API_KEY_REQUIRED=true
# 管理员Key（请设置强密钥）
ADMIN_API_KEY=your-admin-api-key-here

# 限流
RATE_LIMIT_PER_MINUTE=60
RATE_LIMIT_PER_DAY=1000

# 日志
LOG_LEVEL=INFO
LOG_FILE=./data/app.log

# ========================================
# 配置说明
# ========================================
#
# 📝 必须配置的项：
# 1. DEEPSEEK_API_KEY - 在 https://platform.deepseek.com/ 获取
# 2. ADMIN_API_KEY - 生成强密钥（运行下面的命令）
#    python3 -c "import secrets; print('sk-' + secrets.token_urlsafe(32))"
# 3. DATABASE_URL - 选择SQLite或PostgreSQL
#
# 🗄️ 数据库选择：
# - SQLite: 简单，无需额外安装，适合小规模
# - PostgreSQL: 性能好，支持并发，适合生产环境
#
# 📚 PostgreSQL安装步骤：
# 1. 安装: sudo apt install postgresql-16 postgresql-contrib-16
# 2. 创建数据库:
#    sudo -u postgres psql
#    CREATE DATABASE lazy_sheep;
#    CREATE USER lazy_user WITH PASSWORD 'your_password';
#    GRANT ALL PRIVILEGES ON DATABASE lazy_sheep TO lazy_user;
#    \q
# 3. 配置认证: sudo vim /etc/postgresql/16/main/pg_hba.conf
#    host    all    all    127.0.0.1/32    scram-sha-256
# 4. 重启: sudo systemctl restart postgresql
#
# 更多信息请查看: DEPLOY.md
