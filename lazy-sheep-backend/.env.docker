# Docker环境配置

# 应用配置
ENVIRONMENT=development
LOG_LEVEL=INFO

# 数据库配置（连接到postgres容器）
DATABASE_URL=postgresql+asyncpg://lazy_user:lazy_password@postgres:5432/lazy_sheep

# Redis配置（连接到本地Redis）
REDIS_URL=redis://host.docker.internal:6379/0

# API配置
API_HOST=0.0.0.0
API_PORT=8000
API_RELOAD=true

# CORS配置
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080,https://stu.ityxb.com

# 密钥配置（生产环境请修改）
SECRET_KEY=your-secret-key-change-in-production

# AI配置（DeepSeek）
DEEPSEEK_API_KEY=sk-aa76651782494494822d65f743666f80
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-chat
