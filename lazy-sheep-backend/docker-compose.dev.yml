services:
  # PostgreSQL数据库
  postgres:
    image: postgres:15-alpine
    container_name: lazy-sheep-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: lazy_user
      POSTGRES_PASSWORD: lazy_password
      POSTGRES_DB: lazy_sheep
      POSTGRES_INITDB_ARGS: "-E UTF8"
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
      - ./docker/postgres/postgresql.conf:/etc/postgresql/postgresql.conf
    command: postgres -c config_file=/etc/postgresql/postgresql.conf
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U lazy_user"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Redis缓存（如果本地已有Redis，可以注释掉）
  # redis:
  #   image: redis:7-alpine
  #   container_name: lazy-sheep-redis
  #   restart: unless-stopped
  #   ports:
  #     - "6379:6379"
  #   volumes:
  #     - redis-data:/data
  #     - ./docker/redis/redis.conf:/usr/local/etc/redis/redis.conf
  #   command: redis-server /usr/local/etc/redis/redis.conf
  #   healthcheck:
  #     test: ["CMD", "redis-cli", "ping"]
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5

volumes:
  postgres-data:
  # redis-data:

networks:
  default:
    name: lazy-sheep-network
